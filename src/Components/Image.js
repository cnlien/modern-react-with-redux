import React from 'react';
import { Card, CardImg } from 'reactstrap';

const Image = (props) => {

    console.log(props)

    return(
        <div>Hi</div>
        // <Card>
        //     <CardImg key={props.id} src={props.urls.regular} alt={props.description} />
        // </Card>
        
    )

}

export default Image;